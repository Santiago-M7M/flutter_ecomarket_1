name: flutter_ecomarket_1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.27.0  # o la última
  firebase_auth: ^4.17.4  # usa la versión más reciente  # Para autenticación de Firebase
  cloud_firestore: ^4.13.0 #
  google_sign_in: ^6.1.4 # Para iniciar sesión con Google

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/icons/

flutter_icons:
  android: true 
  ios: true
  image_path: "assets/icons/app_logo.png"
  adaptive_icon_background: "#ffffff"
  adaptive_icon_foreground: "assets/icons/app_logo.png"
